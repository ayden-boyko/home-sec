[project]
name = "home-sec-captioning-agent"
version = "0.1.0"
description = "Captioning agent for home security system"
requires-python = ">=3.11,<4.0"
dependencies = [
    "ollama>=0.6.2",
    "numpy>=2.2.6",
]

[tool.uv]
package = false