[project]
name = "home-sec-execution-agent"
version = "0.1.0"
description = "Execution agent with MCP server for home security system"
requires-python = ">=3.11,<4.0"
dependencies = [
    "mcp[cli]>=2.2.0",
    "trycourier>=9.6.0",
    "ollama>=0.6.2",
    "langchain>=1.4.2",
    "langchain-ollama>=1.1.0",
]

[tool.uv]
package = false
