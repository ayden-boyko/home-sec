[project]
name = "home-sec-cam"
version = "0.1.0"
description = "Camera streaming application for home security system"
requires-python = ">=3.11,<4.0"
dependencies = [
    "flask>=3.0",
]

[tool.uv]
package = false